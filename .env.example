# NeuroAdaptive Modules — Environment Variables
# Copy this to .env.local (frontend) and server/.env (backend)

# ── Frontend (.env.local) ──────────────────────────────────

# Supabase Configuration
# Get from: https://app.supabase.io → Your Project → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Google Gemini API
# Get from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=AIzaSyBGEciN55Usz-3uIeUdIrARNnGiTfb9aLE

# Backend Configuration
VITE_BACKEND_URL=http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001/api

# ── Backend Specific (.env placed in /server directory) ────
# NODE_ENV=development
# PORT=3001
# DATABASE_URL=postgresql://user:password@localhost:5432/neuroadaptive
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# ── Backend (server/.env) ──────────────────────────────────
PORT=3001
CLIENT_URL=http://localhost:5174
GEMINI_API_KEY=your_google_gemini_api_key
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret

# ── Integration with Main Project ─────────────────────────
# When integrating, replace the above with your main project's
# existing env values. The backend runs on :3001 to avoid
# conflicting with your main project's :3000 server.
# Change PORT to 3000 and merge the routes when integrating.
